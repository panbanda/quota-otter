{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Local dashboard events only; provider operations are fixed Rust commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
